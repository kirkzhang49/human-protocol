{
  "name": "age-render-webgpu",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Reusable AGE WebGPU renderer slice: contracts, render graph shell, platform/resource abstractions.",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "typecheck": "tsc -p tsconfig.check.json --noEmit",
    "typecheck:runtime": "tsc -p tsconfig.json --noEmit",
    "test:smoke": "tsx tests/smoke/validatePrototype.ts",
    "build": "tsc -p tsconfig.json"
  },
  "devDependencies": {
    "tsx": "^4.19.2",
    "typescript": "^5.6.0"
  },
  "overrides": {
    "esbuild": "0.28.1"
  }
}
